/**
 * soulnet-dsh-sidebar, node half. The package contributes browser presentation
 * only (the SoulMirror sidebar + brand, served as
 * /plugins/soulnet-dsh-sidebar/client.js through `exports["./client"]`); the
 * empty apply gives dsh's Loader the host-side row it keys the client-module
 * scan on.
 */
export function apply(): void {}
